<p id="notice"><%= notice %></p>

<h1>Workplaces</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Host</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user_groups.each do |user_group| %>
      <tr>
        <td><%= user_group.name %></td>
        <td><%= user_group.host.name %></td>
        <td><%= link_to 'Show', user_group %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Workplace', new_user_group_path %>
